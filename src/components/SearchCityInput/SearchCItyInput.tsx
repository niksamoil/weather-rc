import React from 'react';
import StyledInput from './SearchCItyInput.styles';

const SearchCItyInput: React.FC = () => {
  return (
    <div>
      <StyledInput autoFocus />
    </div>
  );
};

export default SearchCItyInput;
